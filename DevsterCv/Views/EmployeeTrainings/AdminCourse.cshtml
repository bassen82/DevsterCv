@model DevsterCv.Models.ViewModels.BindEmployeeTrainingViewModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<meta charset="utf-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1">
<link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

<link href="~/css/list.css" rel="stylesheet">

<script type="text/javascript">
    $(document).ready(function () {
        $('[data-toggle="tooltip"]').tooltip();
    });
</script>


<div class="container">
    <div class="table-wrapper">
        <div class="table-title">
            <div class="row">
                <div class="col-sm-4">

                </div>
                <div class="col-sm-4">
                    <h2 class="text-center">Lägg till <b>Kurs</b></h2>
                </div>
                <div class="col-sm-4">
                    <div class="search-box">

                    </div>
                </div>
            </div>
        </div>

        <form action="/EmployeeTrainings/Add" method="post">
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            Kurser
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < Model.Trainings.Count(); i += 4)
                    {

                        <tr class="tabletrbind">
                            @for (int t = i; t < i + 4; t++)
                            {
                                if (t < Model.Trainings.Count())
                                {
                            <td>
                                @Html.HiddenFor(x => x.Trainings[t].EmployeeId)
                                @Html.HiddenFor(x => x.Trainings[t].TrainingId)
                                @Html.HiddenFor(x => x.Trainings[t].Name)
                                @Html.EditorFor(x => x.Trainings[t].Connected) @Html.DisplayFor(x => x.Trainings[t].Name)
                            </td>
                                }

                            }

                        </tr>

                    }

                </tbody>
            </table>
            <div>
                <div>
                    <input class="btn btn-primary btn-sm" type="submit" value="Spara" /> <a class="btn btn-primary btn-sm" href="@Url.Action("Cv", "Home",  new { id = Model.Trainings[0].EmployeeId })">Tillbaka till Cv</a>
                </div>
            </div>
        </form>
    </div>
</div>



